# apt
alias aptup='sudo apt update && sudo apt upgrade'

# eza
alias ls='eza --icons'
alias tree='ls -T'

# Git
alias gss='git status -s'
alias gs='git status'

# CMake
alias cbuild='cmake -B build && cmake --build build'

# On Android Termux
if [[ "$(uname -s)" == "Linux" && -f /system/build.prop ]]; then
  myhome="/storage/emulated/0"
fi
